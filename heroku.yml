setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile.staging
    review: Dockerfile.staging

release:
  web: yarn migrate:update
  review: yarn migrate:update