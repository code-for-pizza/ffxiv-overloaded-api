setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    staging: Dockerfile.staging

release:
  command: yarn migrage:update
  image: staging

run:
  command: yarn start
  image: staging