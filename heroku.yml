setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    staging: Dockerfile.staging

release:
  command:
    - yarn migrage:update
  image: staging

run:
  server-starter:
    command:
      - yarn start
    image: staging